Resume now lands exactly where you paused instead of up to ten seconds earlier, and a queue you cleared no longer comes back on the next launch. Shuffle starts at the song you picked and keeps every row it was given, duplicates included, through appends, reorders and restores. Queue edits on Android can no longer land on a queue that was still being replaced. New screenshots show the current design.
